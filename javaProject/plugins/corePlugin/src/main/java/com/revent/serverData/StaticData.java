package com.revent.serverData;

/**
 * the static data for the server wich is always the same
 */
public class StaticData {

    // file data
    public static final String itemData = System.getProperty("user.dir") + "serverData/itemData/";

    public static final String questData = System.getProperty("user.dir") + "serverData/questData/";

    public static final String errorData = System.getProperty("user.dir") + "serverData/errors/";

    public static final String langData = System.getProperty("user.dir") + "serverData/locale/";
}
